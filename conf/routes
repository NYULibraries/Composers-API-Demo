# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET 	/									controllers.ComposersController.index
GET     /summary/:resource_identifier       controllers.ComposersController.summary(resource_identifier)
GET		/detail/:cuid						controllers.ComposersController.detail(cuid)
GET		/archiveit/:resource_identifier 	controllers.ComposersController.archiveit(resource_identifier)
GET		/authenticate/:controller/:identifier 	controllers.ComposersController.authenticate(controller, identifier)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
